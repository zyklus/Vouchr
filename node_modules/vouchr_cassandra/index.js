var cassandra = require('cassandra'),
       config = require('./../../config').database;

var client = new cassandra.Client(config.host + ':' + config.port);

client.consistencyLevel({
	write : config.consistency.write,
	read  : config.consistency.read
});

client.connect(config.keyspace);

module.exports = client;